import { EmployeeLoginModel } from './employee-login-model.model';

describe('EmployeeLoginModel', () => {
  it('should create an instance', () => {
    expect(new EmployeeLoginModel()).toBeTruthy();
  });
});
