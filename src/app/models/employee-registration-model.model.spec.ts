import { EmployeeRegistrationModel } from './employee-registration-model.model';

describe('EmployeeRegistrationModel', () => {
  it('should create an instance', () => {
    expect(new EmployeeRegistrationModel()).toBeTruthy();
  });
});
